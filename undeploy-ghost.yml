---
# Playbook to undeploy Ghost and its component

- name: Undeploy Ghost
  hosts: localhost
  gather_facts: false
  vars:
    delete_data: "false"
  roles:
    - ghost_undeploy
    - { role: mysql_undeploy, when: delete_data == "true"}